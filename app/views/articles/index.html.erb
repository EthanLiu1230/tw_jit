<p id="notice"><%= notice %></p>

<div class="flex items-center">
  <h1 class="flex-1 text-3xl font-bold">
  <span>
  <%= @q.result.count %>
  </span>
    Articles
  </h1>

  <%= search_form_for(@q, class: 'form-control my-2') do |f| %>
    <div class="relative w-full">
      <%= f.search_field :title_cont, class: 'w-full pr-16 input input-primary input-bordered' %>
      <%= f.submit name: '', class: 'absolute right-0 top-0 rounded-l-none btn btn-primary' %>
    </div>
  <% end %>
</div>

<div class="overflow-x-auto">
  <table class="table table-zebra w-full">
    <thead>
    <tr>
      <th>Title</th>
      <th>Author</th>
      <th>Publish date</th>
      <th colspan="3"></th>
    </tr>
    </thead>

    <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= article.title %></td>
        <td><%= article.author.name %></td>
        <td><%= article.publish_date %></td>
        <td><%= link_to 'Show', article %></td>
        <td><%= link_to 'Edit', edit_article_path(article) %></td>
        <td><%= link_to 'Destroy', article, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>
<%= paginate @articles %>

<br>

<%= link_to 'New Article', new_article_path %>
